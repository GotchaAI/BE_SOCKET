version: '3.8'
services:
  app:
    image: jeongminju45/gotcha-socket-server:latest
    ports:
      - "8080:8080"
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
